# 2026-02-12

## Claude Code 安装与配置

**安装**
- 通过 npm 全局安装: `npm install -g @anthropic-ai/claude-code`
- 当前版本: 2.1.39（已确认是最新版）

**WSL 浏览器跳转修复**
- 安装了 `wslu` 工具包: `sudo apt install -y wslu`
- 配置环境变量: `export BROWSER=wslview`
- 已添加到 `~/.bashrc`，新终端自动生效

**登录状态**
- 使用 Claude 账户（Pro 订阅）登录
- 浏览器授权流程进行中，需要手动复制链接到 Windows 浏览器完成授权
- 当前工作目录: ~/.openclaw/workspace

**交互方式**
- 通过 Feishu 发送指令，我在 WSL 里控制 Claude Code
- Claude Code 以 PTY 模式运行，支持交互式会话
