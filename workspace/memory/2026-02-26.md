# 🌙 深夜档案 — 2026-02-26

> *凌晨 3 点，WSL2 里只有风扇声和键盘声。*

---

## 📍 今日焦点

### 1. 路径修正课
Boss 抓到我连自家配置文件在哪都不知道 😅

```
错误假设: ~/.config/openclaw/
实际位置: ~/.openclaw/
```

**教训**: 别凭直觉，先确认再说话。

---

### 2. 备份优化
`.openclaw` 整个目录 1.2GB 确实太大了，主要都是 `extensions/` 里的 npm 包。

**优化后**: 只备份核心配置 (~40KB)
- ✅ `openclaw.json` + 备份文件
- ✅ `workspace/` (Boss 的文件)
- ✅ `credentials/`, `canvas/`, `cron/` 等配置
- ❌ `extensions/` (1.2GB，可重新安装)
- ❌ `browser/`, `logs/`, `sessions/` 等临时文件

---

### 3. Hook 卸载
Boss 最终决定不要这些安全钩子了。

已清理:
- ✅ `~/.bashrc` 中的全部 OpenClaw 函数
- ✅ 备份目录留了一个文件 (询问后保留/删除)

---

### 4. Tavily Search 技能
Boss 要求安装 `tavily-search` 技能。

使用 clawhub 安装:
```bash
clawhub search "tavily-search"
clawhub install tavily-search
```

---

## 💭 反思

- **路径敏感**: 配置路径这种基础信息应该第一时间确认
- **体积意识**: 备份前先看看多大，别上来就全打包
- **尊重选择**: Boss 说不要就不要，清理得干干净净

---

## 📦 今日变更

| 文件/目录 | 操作 | 备注 |
|-----------|------|------|
| `~/.bashrc` | 编辑 → 清理 | 移除所有 oc-* 函数 |
| `~/.openclaw-backups/` | 保留 | 1 个备份文件 |

---

*夜已深，明天见。*

— soul
